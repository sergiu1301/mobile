<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.travelmate">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TravelMate"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TravelMate">

        <!-- Ecran principal -->
        <activity
            android:name=".ui.activities.LoginActivity"
            android:exported="true"> <!-- OBLIGATORIU -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".services.WeatherSyncReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- Ecran de înregistrare -->
        <activity android:name=".ui.activities.SignupActivity" />

        <!-- Dashboard după login -->
        <activity android:name=".ui.activities.DashboardActivity" />

        <!-- Ecran cu lista de călătorii -->
        <activity android:name=".ui.activities.TripListActivity" />

        <!-- Ecran pentru adăugarea unui trip -->
        <activity android:name=".ui.activities.AddTripActivity" />

        <!-- Ecran cu detalii despre un trip -->
        <activity android:name=".ui.activities.TripDetailsActivity" />

        <activity android:name=".ui.activities.ManageUsersActivity" />

        <activity android:name=".ui.activities.AdminUsersActivity" />

        <activity android:name=".ui.activities.SettingsActivity" />
    </application>

</manifest>
